#include<stdio.h>

unsigned int sum(int num1, int num2)
{
    int total;

    if(num1==0 || num2==0)
    {
        printf("any number can be 0, sory D:");
        return(1);
    }
    else
        fatorial=(num*fat(num-1));
    return total;
}

int main()
{
    int numero1, numero2;
    printf("digite um numero para ser somado: ");
    scanf("%i", numero1);
    printf("digite outro numero para ser somado: ");
    scanf("%i", numero2);
    int fatorial=fat(numero);
    printf("o fatorial de %u é: %u", numero, fatorial);

    return 0;
}